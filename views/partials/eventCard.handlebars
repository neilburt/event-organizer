
<div class="card">
  <div class="card-body">
    <h3 class="card-title" id="event-title">{{title}}</h3>
    <p class="card-text" id="event-type">Event type:  {{type}}</p>
    <p class="card-text" id="event-date"> Date and time:  {{format_date datetime_local}}</p>
    <a href={{url}} target="_blank" class="btn btn-primary">Link to the Event!</a>
    <button class="saveBtn btn btn-primary">Save Me!</button>
  </div>
</div>

{{!-- <div>
    <h2>{{title}}</h2>
    <p>{{description}}</p>
    <p>Event By {{}} on {{}}</p>
            <p>
                <span for="img" aria-label="money">$</span>
                <span class="dollar-amount">{{format_amount needed_entry}}</span>
                needed
            </p>
           
</div> --}}